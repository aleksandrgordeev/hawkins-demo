### Hawkins library demo

I had some problems understanding how to change the default styles of hawkins components so I've crearted a small demo with possible approach how to do it.

```
npm install
npm start
```